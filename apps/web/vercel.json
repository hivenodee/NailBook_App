{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile --filter web...",
  "buildCommand": "pnpm --filter @nailbook/db exec prisma generate && next build"
}
